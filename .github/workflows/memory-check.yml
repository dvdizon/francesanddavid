name: Memory Check

on:
  pull_request:

jobs:
  memory-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Run memory check
        run: bash scripts/memory_check.sh
